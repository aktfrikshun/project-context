<!-- Purpose: Indexes project contexts and defines project identifier conventions. -->
# Projects

Each real project has one lowercase kebab-case directory and a consistent set of documents. Copy [`_template`](_template/) to begin.

## Registry

| Project ID | Name | Status | Owner | Overview |
| --- | --- | --- | --- | --- |
| _None yet_ |  |  |  |  |

The registry is the discovery surface for humans and future `load_project_context(project)` implementations. Add every project here in the same change that creates its directory.
